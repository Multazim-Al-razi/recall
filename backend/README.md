# Recall Backend

> Placeholder for future backend services.

## Planned

- **API Server** — REST/GraphQL API for cloud sync, multi-device support
- **Auth** — User authentication (email + OAuth)
- **Database** — Subscription data persistence
- **Notifications** — Email/push renewal reminders

## Status

Not yet implemented. Recall v1 is a **local-first** client-side SPA with no backend dependency. Backend services will be added in v2+ for cloud sync and multi-device support.