# Recall frontend configuration (copy to .env for local overrides)

# Supabase project URL (from Settings > API in the Supabase dashboard)
VITE_SUPABASE_URL=

# Supabase anon/public key (from Settings > API in the Supabase dashboard)
VITE_SUPABASE_ANON_KEY=
